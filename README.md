This project has been inspired from https://github.com/skills/github-pages.

But because of limitations, a new repo was created with minima dependencies using Gemfile.

# Rake task for creating new posts

Run this `rake` command for creating new post.

```sh
rake "Title"
```